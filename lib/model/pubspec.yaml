name: model
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  intl:
  flutter:
    sdk: flutter
  path:
  sembast:
  sembast_web:
  settings:
    path: '../../lib/settings'

dev_dependencies:
  lints:
  test:

flutter:
  assets:
    - '../../assets/datasets/customers.json'
    - '../../assets/datasets/plans.json'
    - '../../assets/datasets/machines.json'
